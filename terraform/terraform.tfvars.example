## Example terraform.tfvars for cloud-1
# Copy this to `terraform/terraform.tfvars` and adjust values before running

aws_region     = "us-east-1"
aws_profile    = "default"   # Optional: use named AWS CLI profile or leave empty to use env vars
instance_type  = "t3a.small"
key_name       = ""          # Name of an existing AWS KeyPair in your account (leave empty to use aws_key_pair resource)
# public_key_path = "/home/you/.ssh/cloud1_id_ed25519.pub"  # Required only if creating a key pair via Terraform

# Notes:
# - If you set `key_name` to an existing KeyPair, ensure you have the private key locally
#   and provide its path to Ansible (ansible_ssh_private_key_file) when updating inventory.
# - If you prefer Terraform to upload your public key, set `public_key_path` and
#   uncomment the `aws_key_pair` resource in `terraform/main.tf`.// Example terraform.tfvars for cloud-1
// Copy this to terraform/terraform.tfvars and edit values before running terraform apply.

aws_region    = "us-east-1"
aws_profile   = ""                // Optional: set to your AWS CLI profile name
instance_type = "t3a.small"
key_name      = ""                // If you already have an AWS KeyPair, set its name here; otherwise keep empty and create/upload a key
public_key_path = "/home/youruser/.ssh/cloud1_id_ed25519.pub" // Used only if you enable aws_key_pair in main.tf

